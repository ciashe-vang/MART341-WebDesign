DNS is a system that provides an association of IP addresses
Cookies are small files that most often times include unique identifiers that web servers send to browsers.
The three parts to a URL is the protocol, the domain name, and the path that leads to a specific web page.
